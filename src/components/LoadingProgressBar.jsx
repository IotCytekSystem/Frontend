import React from 'react';

const LoadingProgressBar = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-2 bg-blue-500">
      {/* Add animation or style here */}
    </div>
  );
};

export default LoadingProgressBar;
